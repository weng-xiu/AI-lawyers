<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ai.lawyers.system.mapper.lawyers.AiChatMessageMapper">

    <resultMap type="AiChatMessage" id="AiChatMessageResult">
        <result property="messageId"      column="message_id"      />
        <result property="sessionId"      column="session_id"      />
        <result property="senderType"     column="sender_type"     />
        <result property="senderName"     column="sender_name"     />
        <result property="content"        column="content"         />
        <result property="msgType"        column="msg_type"        />
        <result property="attachmentUrl"  column="attachment_url"  />
        <result property="isRead"         column="is_read"         />
        <result property="sendTime"       column="send_time"       />
        <result property="createBy"       column="create_by"       />
        <result property="createTime"     column="create_time"     />
        <result property="remark"         column="remark"          />
    </resultMap>

    <sql id="selectAiChatMessageVo">
        select message_id, session_id, sender_type, sender_name, content, msg_type, attachment_url,
               is_read, send_time, create_by, create_time, remark
        from ai_chat_message
    </sql>

    <select id="selectAiChatMessageList" parameterType="AiChatMessage" resultMap="AiChatMessageResult">
        <include refid="selectAiChatMessageVo"/>
        <where>
            <if test="sessionId != null"> and session_id = #{sessionId}</if>
            <if test="senderType != null and senderType != ''"> and sender_type = #{senderType}</if>
            <if test="senderName != null and senderName != ''"> and sender_name like concat('%', #{senderName}, '%')</if>
            <if test="msgType != null and msgType != ''"> and msg_type = #{msgType}</if>
            <if test="isRead != null and isRead != ''"> and is_read = #{isRead}</if>
        </where>
        order by send_time asc
    </select>

    <select id="selectAiChatMessageByMessageId" parameterType="Long" resultMap="AiChatMessageResult">
        <include refid="selectAiChatMessageVo"/>
        where message_id = #{messageId}
    </select>

    <insert id="insertAiChatMessage" parameterType="AiChatMessage" useGeneratedKeys="true" keyProperty="messageId">
        insert into ai_chat_message
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="sessionId != null">session_id,</if>
            <if test="senderType != null and senderType != ''">sender_type,</if>
            <if test="senderName != null and senderName != ''">sender_name,</if>
            <if test="content != null">content,</if>
            <if test="msgType != null and msgType != ''">msg_type,</if>
            <if test="attachmentUrl != null and attachmentUrl != ''">attachment_url,</if>
            <if test="isRead != null and isRead != ''">is_read,</if>
            <if test="sendTime != null">send_time,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            <if test="remark != null and remark != ''">remark,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="sessionId != null">#{sessionId},</if>
            <if test="senderType != null and senderType != ''">#{senderType},</if>
            <if test="senderName != null and senderName != ''">#{senderName},</if>
            <if test="content != null">#{content},</if>
            <if test="msgType != null and msgType != ''">#{msgType},</if>
            <if test="attachmentUrl != null and attachmentUrl != ''">#{attachmentUrl},</if>
            <if test="isRead != null and isRead != ''">#{isRead},</if>
            <if test="sendTime != null">#{sendTime},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            sysdate()
        </trim>
    </insert>

    <update id="updateAiChatMessage" parameterType="AiChatMessage">
        update ai_chat_message
        <trim prefix="SET" suffixOverrides=",">
            <if test="sessionId != null">session_id = #{sessionId},</if>
            <if test="senderType != null and senderType != ''">sender_type = #{senderType},</if>
            <if test="senderName != null and senderName != ''">sender_name = #{senderName},</if>
            <if test="content != null">content = #{content},</if>
            <if test="msgType != null and msgType != ''">msg_type = #{msgType},</if>
            <if test="attachmentUrl != null and attachmentUrl != ''">attachment_url = #{attachmentUrl},</if>
            <if test="isRead != null and isRead != ''">is_read = #{isRead},</if>
            <if test="sendTime != null">send_time = #{sendTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where message_id = #{messageId}
    </update>

    <delete id="deleteAiChatMessageByMessageId" parameterType="Long">
        delete from ai_chat_message where message_id = #{messageId}
    </delete>

    <delete id="deleteAiChatMessageByMessageIds" parameterType="String">
        delete from ai_chat_message where message_id in
        <foreach item="messageId" collection="array" open="(" separator="," close=")">
            #{messageId}
        </foreach>
    </delete>

    <update id="markReadBySessionId" parameterType="Long">
        update ai_chat_message set is_read = '1' where session_id = #{sessionId} and is_read = '0' and sender_type = '1'
    </update>
</mapper>
