-- ============================================================
-- 工作台菜单整合（可回滚）
-- 统一工作台已改为标签页，内置“概览 / 待办 / 公告”
-- 隐藏侧边栏中重复的独立菜单入口，保留路由与权限
-- ============================================================

UPDATE sys_menu
SET visible = '1'
WHERE menu_id IN (3632, 3638);

-- 回滚：
-- UPDATE sys_menu SET visible = '0' WHERE menu_id IN (3632, 3638);
