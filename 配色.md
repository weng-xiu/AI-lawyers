针对**12348公共法律服务热线**后台系统，设计配色方案的核心逻辑在于平衡**“法治的权威严肃”**与**“公共服务的亲民温暖”**。12348特有的**司法蓝**与**徽章金**是视觉灵魂，但后台系统需额外注重**长时间操作的护眼性**与**信息层级的高效识别**。

以下为您定制一套完整的后台系统配色设计方案（附色值规范）：

---

### 一、核心设计理念（Design Token）
*   **基调**：肃穆、信赖、清晰。
*   **关键词**：法治温度、数字正义、高效响应。
*   **对比度**：全系配色需满足 WCAG 2.1 AA 级无障碍标准（文字对比度 ≥ 4.5:1）。

---

### 二、品牌主题色（Brand Colors）
**采用“深司法蓝 + 品质金”组合，奠定权威基调。**

| 角色 | 色值名称 | Hex 色号 | 使用场景 |
| :--- | :--- | :--- | :--- |
| **主色调** | **深邃法蓝** | `#1A3C6E` | 顶部导航栏、一级菜单背景、重要按钮默认态、登录页主背景。 |
| **主色调悬浮** | **明亮法蓝** | `#255A99` | 导航栏悬浮态、主要按钮 Hover 态、卡片边框强调。 |
| **辅色调** | **法治之光金** | `#C9A96E` | **核心点缀色**：Logo 徽章、当前选中菜单左侧装饰条、待办事项角标、重要数据看板高亮。 |
| **辅色调浅** | **淡金辉光** | `#F3EDE0` | 选中菜单的背景底色、重要提示横幅背景。 |

---

### 三、功能语义色（Functional Colors）
针对后台工单状态（咨询中、转办中、已办结、超时、退回）进行严格区分。

| 状态 | 色值名称 | Hex 色号 | 应用场景（工单状态标签） |
| :--- | :--- | :--- | :--- |
| **成功/已完成** | **稳重绿** | `#2B8C6E` | 已办结、已回访、已归档。 |
| **进行中** | **信息蓝** | `#2B6BB0` | 咨询中、通话中、转办中。 |
| **警告/待处理** | **提醒橙** | `#E8923A` | 即将到期、待分配、待审核。 |
| **危险/超时** | **肃正红** | `#C63D4A` | 工单超时、投诉升级、紧急预警。 |
| **暂停/挂起** | **中性灰** | `#8C8C8C` | 挂起、无效工单。 |

---

### 四、中性色板（Neutral / Grayscale）
后台系统 90% 的面积由中性色构成，本方案侧重**低饱和度、高可读性**。

| 层级 | 色值名称 | Hex 色号 | 使用规范 |
| :--- | :--- | :--- | :--- |
| **主文本** | **极深铁灰** | `#1F2A3A` | 正文标题、表格主要数据（对比度最高）。 |
| **次要文本** | **中灰** | `#5A6A7E` | 辅助说明文字、标签描述。 |
| **占位/禁用** | **浅灰** | `#B0BCCA` | 输入框占位符、禁用按钮、分割线。 |
| **背景色** | **纯净白/霜灰** | `#FFFFFF` / `#F5F7FA` | 页面主背景（纯白）、卡片背景（霜灰分隔）。 |
| **边框** | **冷调边框** | `#DCE2EB` | 表格边框、输入框边框、分割卡片。 |

---

### 五、数据可视化（图表）配色方案
针对后台“话务量分析”、“满意度走势”、“区域热力图”等组件：

> **原则**：在司法蓝基调上，搭配低饱和度、易于区分的邻近色与对比色。

- **主序列色**：`#1A3C6E` → `#3B73B3` → `#6A9FD8` → `#A9C9EB`（单色渐变）。
- **对比强调色**：`#C9A96E`（用于突出最高峰值或重要KPI）。
- **辅助序列**：`#54A68B`（绿）、`#E8923A`（橙）、`#9B6EAA`（紫灰）、`#D98C8C`（粉棕）。

---

### 六、典型 UI 模块应用规范（落地指导）

#### 1. 顶部导航栏（Header）
- **背景**：`#1A3C6E` 纯色（不建议加透明度，避免内容干扰）。
- **Logo 文字**：纯白 `#FFFFFF`，**“12348”** 数字单独使用 **金色 `#C9A96E`** 加粗。
- **用户信息区**：白色 `#FFFFFF` 文字，头像背景使用金色渐变。

#### 2. 侧边菜单栏（Sidebar）
- **展开背景**：`#F8F9FC`（浅灰蓝），**选中态**：左侧金色竖条（`#C9A96E` 宽 3px），文字变为 `#1A3C6E` 并加粗。
- **缩起图标**：默认 `#5A6A7E`，悬停 `#1A3C6E`。

#### 3. 工单列表（Table / List）
- **斑马纹**：`#FFFFFF` 与 `#FAFBFD` 交替。
- **表头背景**：`#EDF0F5`（冷调中性色）。
- **优先级标签**：紧急工单左侧带 **红色** (`#C63D4A`) 竖条装饰。

#### 4. 按钮体系（Buttons）
- **主要按钮（Primary）**：背景 `#1A3C6E`，悬停 `#255A99`，文字白。
- **次要按钮（Default）**：边框 `#B0BCCA`，文字 `#5A6A7E`，悬停边框 `#1A3C6E`，文字 `#1A3C6E`。
- **强调按钮（CTA）**：背景 `#C9A96E`（金色），文字 `#1F2A3A`（深色），悬停背景 `#D9BF8A`（用于“一键转办”或“发布公示”等权威操作）。

---

### 七、深色模式适配建议（选配）
若需支持夜间值班模式，请按以下规则映射：
- **背景**：`#0D1626`（替代白色）。
- **卡片**：`#18243B`（带微弱蓝底）。
- **文字**：`#E6EDF5`（主）、`#99A9C4`（次）。
- **金色保留**：`#C9A96E` 在深色背景下需加亮 10%（变为 `#DCC28A`）以保证可见度。

---

### 八、设计源文件参考（心理映射）
在 Figma / Sketch 中定义变量（Variables）时，建议命名为：
- `--color-brand-deep` (深蓝)
- `--color-brand-gold` (徽章金)
- `--color-status-overtime` (超时红)